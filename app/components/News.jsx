var React = require('react');
var UsersList = require('UsersList');

var News = React.createClass({
    render() {
        return (
            <div>
                News component
            </div>
        );
    }
});

module.exports = News;
